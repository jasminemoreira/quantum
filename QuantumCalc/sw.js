// sw.js — App Shell service worker da PWA do Quantum Calculator (v10).
// Estratégia (specs/technical/22 §Resolução Fase 3): PRECACHE ENXUTO dos pontos de entrada + FETCH
// cache-first COM cache-on-fetch — toda resposta GET same-origin nova é guardada na 1ª visita online
// (KaTeX vendorizado, fontes, etc.), de modo que o precache não precisa listar todo path do shell.
// Cache VERSIONADO: um bump de CACHE faz o activate purgar a versão anterior (update flow padrão).
// Só roda quando servido por http(s); em file:// o registro é guardado e o arquivo abre como página simples.
const CACHE = 'qcalc-v21-43';  // v21-43: card A3 (QFT) agora PAREIA forma MANUAL (didática, H·CP·SWAP) com a TECLA de alto nível (ALL QFT) p/ |000⟩ e |001⟩ — estados capturados IDÊNTICOS (prova visual da equivalência) — + QFT† como inverso (QFT·QFT†→|001⟩). Novo diagrama de circuito QFT₃ (op 'swap' = ×—× adicionada ao renderizador). Pedido da operadora: ter as duas versões no mesmo exemplo. // v21-42: trava orientação retrato (manifest + lock JS) Primário = manifest "orientation":"portrait" (PWA instalada/standalone; navegador comum não é afetado). + lock JS best-effort screen.orientation.lock('portrait') no init (efeito imediato em standalone, sem reinstalar; rejeição em navegador/iOS/sem-fullscreen é ignorada). Manifest está no SHELL precache (cache-first) → este bump força o refetch. // v21-41: fidelidade das teclas no cookbook (stepsToKeys derivado dos steps). A operadora viu prep omitida ("…"), atalhos não-literais ("Bell"/"GHZ") e PSEUDO-teclas inexistentes ("(I)", "(no oracle)") + um box "Bᵢ" sem explicação no circuito. As strings de keys eram ESCRITAS À MÃO e divergiam dos steps reais. FIX: a sequência exibida agora é DERIVADA dos steps (stepsToKeys) — fonte única de verdade; cada chip é uma tecla real, prep sempre completa. "(no oracle)" fica só nos RÓTULOS (descreve o caso, não é tecla). Box do superdense Bᵢ→"enc" + legenda (I/X/Z/ZX de Alice). // v21-40: fix do diagrama Grover (colW adaptativa) (E4) "empilhado" — boxes 'Oracle'/'Diffuser' (≈59/76px) excediam a colW FIXA de 52px → encavalavam e cortavam ("Oracl"). circuitSVG agora usa largura de coluna ADAPTATIVA: colW = max(footprint da coluna) + gap(18); centros acumulados. Vale p/ todos os circuitos (QFT†, U⁴, etc.). // v21-39: revisão do manual (Q-omission, teleporte completo, 9 diagramas de circuito) (só doc; runtime quantum_calc.html INTOCADO). (1) §4 documenta a OMISSÃO DO Q — dígito solto antes de porta/op de 1 qubit já é o alvo (0 H ≡ 0 Q H); Q só p/ separar operandos. (2) Card A4 (teleporte) completado até a MEDIÇÃO (passo 0 Q measure · 1 Q measure, amostrado) + correção de Pauli explicada no why/§10. (3) DIAGRAMAS DE CIRCUITO em SVG inline (offline) — renderizador por colunas em examples-render.mjs + 9 specs: Bell(I1) GHZ(I2) teleporte(A4) superdense(A5) Deutsch(E1) DJ(E2) BV(E3) Grover(E4) QPE(E5). // v21-38: faixa vazia embaixo — Math.max das 3 fontes de viewport PERSISTE após o v21-37 (salva de re-tentativas não bastou) ⇒ a fonte da medição (documentElement.clientHeight) retorna valor ESTÁVEL porém ERRADO (menor) no resume/bfcache; re-medir a mesma fonte não corrige. AGORA avail = Math.max(clientHeight, innerHeight, visualViewport.height) — a fonte correta vence sem encolher por causa de uma stale-pequena; teto = tela real. + visualViewport 'scroll' (barras assentando) dispara re-fit; salva estendida a 2500ms. // v21-37: salva de re-tentativas em todo show/resume ao voltar do manual (swipe-back) ou retomar o app após um tempo. Causa: fitViewport TRAVA o clientHeight do instante em que roda; no resume/bfcache o WebView reporta clientHeight transitoriamente MENOR (barras do sistema/transição assentando) → display/app curto → faixa. Antes só o `load` disparava a salva de re-tentativas (rAF+120/400/900/1600ms); resume/pageshow/focus/visibilitychange faziam UMA medição (80ms) e latcheavam o valor errado. Agora TODO evento de show/resume dispara a mesma salva (scheduleFit), e a medida final (correta) vence. // v21-36: REMOVIDO o auto-return da pág.2 → pág.1 após aplicar porta/preset (escolha da operadora). O teclado PERMANECE na página corrente, permitindo ENCADEAR portas da cauda-longa (SWAP→CCX→S†…) sem re-swipar; volta à pág.1 só por swipe/seta/dot. Cookbook/manual atualizados (page:0 explícito nas sequências que voltavam à pág.1: Grover multi-iteração, QPE/E5). // v21-35: FIX DEFINITIVO do "háptico dispara mas a tecla não tem efeito; preciso esperar ~1s" pós-swipe. CAUSA REAL (revelada pela pista do háptico = pointerdown OK): durante a transição CSS do strip (.24s) o botão DESLIZA de baixo do dedo PARADO entre o pointerdown e o pointerup → o navegador NÃO gera o synthetic `click` (down-target ≠ up-target) → tecla sem efeito; o háptico (pointerdown) dispara. FIX: as teclas do carrossel são roteadas SEMPRE no POINTERUP (a partir de downActionEl, o botão sob o dedo no pointerdown), nunca dependendo do click. O handler de click IGNORA alvos dentro do #viewport (evita duplicação) e roteia só o que está fora (comando M/undo/redo, setas do display, dots, Bloch). Sem janela temporal de suppressClick p/ taps (engolia o próximo clique legítimo). // v21-34: tap apressado c/ jitter (operadora: precisava esperar 1-2s). Causa: ao tocar rápido o dedo ainda está em leve movimento; o pointermove passava do limiar de 10px e TRAVAVA o toque como micro-swipe → engolido. Correções: (1) SWIPE_AXIS_LOCK 10→16px (tolera jitter de tap apressado); (2) se o gesto TRAVA mas NÃO viaja o suficiente p/ trocar de página (< SWIPE_COMMIT), trata como TAP: roteia downActionEl (botão sob o dedo no pointerdown, imune ao drift) no pointerup — confiável no device, sem depender do click. // v21-33: FIX GERAL do "1º tap engolido na pág.2" (Bell/⟨ZZ⟩/QFT/qualquer botão) após swipe. Causa: o suppressClick por janela de 80ms era uma CORRIDA no device real (ordem synthetic-click↔pointerdown imprevisível); o teste v21-29 passava no Playwright (eventos determinísticos) mas o celular falhava. Correção INDEPENDENTE DE TEMPO: o handler de click compara a posição do release com a do último pointerdown (clickDownX/Y) — release longe do down (>SWIPE_AXIS_LOCK num eixo) = veio de arrasto/swipe → descarta; tap no lugar = dispara. suppressClick mantido como guarda secundária (gaveta de ângulo). // v21-32: NEW OP ⟨ZZ⟩ (page 2, operations) — quantum correlation = exact parity expectation ⟨⊗Z⟩ in Z[zeta16]. Ops.corr(s,qubits): empty/ALL spans all qubits; n Q gives single-qubit ⟨Zn⟩=P0−P1; i Q j Q gives the CHSH two-qubit ⟨Zi Zj⟩=P00+P11−P01−P10. onOp('corr') reads Parser.peekOperands().targets, labels with per-qubit subscripts, prints exact rect value + decimal. concrete-only. CHSH block H·T·H → ⟨Z⟩=1/√2 EXACT. // v21-31: ESC (key:CLR, outside angle/lambda entry) now also dismisses the lingering detail-pane message (e.g. 'Result phi tensor psi became the current state') by clearing the aux output and returning to the state pane (displayPane 0). prob (a toggle) re-renders on refresh; only informational messages are closed. // v21-30: tap-to-shrink state font 50% to 70%.
// CACHE_v19-2:  // v19-2: FIX PWA collapse — fitViewport setava disp.style.height dinâmico ('400px' no cel), e ao colapsar eu limpava com '' → voltava ao CSS 172px estático (menor que original). Agora salvo _preExpandDispH no expand, restauro no collapse (lição v19-L11). // v19-1: 3 frentes — F1 CONDENSE CONCRETO (Bell `(1/√2)|00⟩+(1/√2)|11⟩` → `(|00⟩+|11⟩)/√2` etc.; predicado 6 cond. + threshold ≤4 termos lição v19-L1); F2 DENOM_ALT const única (DRY anti-drift lição v18-L3); F3 nova UX do display — setas laterais ◀▶ (substituem dots; tap zones 22px no cantinho c/ padding 17px), seta inferior ▼/▲ de EXPANDIR (oculta zones kets/gates/gate-variants/2qubits, anima sincronizada via max-height+opacity+padding+margin transitions, app.style.height fixa durante .27s p/ evitar pulo da borda lições v19-L9/L10); scrollbars do display+página ocultas. 428 testes (320 Node + 108 PW). v18-1 cache fica como histórico abaixo. // v18-1: CONDENSE de coef simbólico composto no display (½+½·e^{iθ}→(1+e^{iθ})/2, (1/√2)±(1/√2)e^{iθ}→(1±e^{iθ})/√2) — SymExpr.condense + format + 1 regra toKatex `(num)/d→\\dfrac`. Predicado de 7 condições (≥2 mono · todo amp.ex · g real positivo · amp_k∈{g,−g} eq EXATA ζ₁₆ · 1/g≠1 · denom limpo · phase coefD=1). Recursão prova-termina em 1 nível. 411 testes (306 Node + 105 PW, +13 v18 + 5 migrações intencionais v4×4/ui.spec×2). 0 padrão/módulo novo. Bug arquitetural (den deve ser SEMPRE 'rect', não fmt do usuário) pego em P5 e fixado. v17-1: FIX symBloch — routeAction barrava s.sym antes do showBloch (a esfera de qubit concreto separável não abria); agora delega ao showBloch (que trata concreto E simbólico-separável + msg de recusa). v17-0: symBloch (esfera de Bloch de qubit concreto separável num SymState, ex.: |0⟩⊗|ψ⟩, Q0→+z) + fold automático ψ₀|0⟩+ψ₁|1⟩→|ψ⟩ (clímax do teleporte; o qubit vira ket abstrato de MOTOR, reutilizável). iOS KaTeX baseline DESCOPADO (radar). // v16-16: manual.html atualizado (§1 visor de 2 painéis: estado|detalhe, tap/dots/auto-switch, Bloch inline). v16-15: chip rad/turns na entrada. (runtime quantum_calc.html inalterado desde v16-15) (inline, didático: estado+esfera juntos); detalhe = só readouts de texto. v16-3: AUTO-SWITCH animado — op com resultado (Bloch/prob/measure/Schmidt/…) rola sozinha p/ o detalhe (M/⊗ não); + fix dos dots no Bloch (showBloch agora reconcilia applyDisplayPane). v16-2: fix do 2º painel — strip absoluto (altura por top/bottom:0, sem height:100% que colapsa no WebView) + touch-action:pan-y nos painéis + setPointerCapture ao travar o swipe (sem snap-back indevido). v16-1: VISOR de 2 painéis — principal = só o estado; SWIPE-LEFT (ou dots) → detalhe (readouts de prob/measure/Bloch/Schmidt/… + esfera de Bloch); affordance só quando há detalhe; volta ao principal ao mudar o estado. UI-only (out()/motor intocados). v15-4: gaveta de ângulo
const SHELL = [
  './quantum_calc.html',
  './manual.html',
  './manifest.webmanifest',
  './icons/icon.svg',
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE).then((c) => c.addAll(SHELL)).then(() => self.skipWaiting())
  );
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys()
      .then((keys) => Promise.all(keys.filter((k) => k !== CACHE).map((k) => caches.delete(k))))
      .then(() => self.clients.claim())
  );
});

self.addEventListener('fetch', (event) => {
  const req = event.request;
  if (req.method !== 'GET') return;
  const url = new URL(req.url);
  if (url.origin !== self.location.origin) return;   // só same-origin (evita guardar respostas opacas)
  event.respondWith(
    caches.match(req).then((hit) => {
      if (hit) return hit;                            // cache-first
      return fetch(req).then((res) => {
        if (res && res.ok && res.type === 'basic') { // cache-on-fetch: guarda a resposta same-origin nova
          const copy = res.clone();
          caches.open(CACHE).then((c) => c.put(req, copy));
        }
        return res;
      }).catch(() => hit);                            // offline e sem cache → erro de rede
    })
  );
});
